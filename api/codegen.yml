overwrite: true
schema: 'http://localhost:4000/graphql'
documents: null
generates:
  src/graphql/schema/codegen/generated.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
    config:
      useIndexSignature: true
      mappers:
        Player: '../../../db/entities#PlayerModel'
